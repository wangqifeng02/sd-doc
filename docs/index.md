---
hero:
  title: Alibaba Cloud Spark Design
  description: An Basic UI Component
---

欢迎来到 Spark Design 组件库。

如果没有自动跳转，请前往组件总览页面。

[前往组件](/#/components)

<script>
  (function () {
    try {
      var hash = location.hash || '';
      // 仅在没有明确路由时跳转，避免影响已有锚点或路由
      if (!hash || hash === '#/' || hash === '#') {
        location.hash = '#/components';
      }
    } catch (e) {}
  })();
</script>
